Based on the current symbolic policy, after training for {train_steps} environment steps:
- Success rate: {win_rate}
- Mean episode reward (environment reward): {mean_reward}

Evaluation details across multiple episodes:
{current_output}

Please carefully analyze the policy feedback and propose structural improvements. Some helpful tips:

(1) If the success rate is always near zero, the policy structure likely needs fundamental changes — reconsider the phase decomposition, observation index usage, or the overall symbolic expression structure. Rewrite the entire policy.

(2) If the agent reaches the object but fails to manipulate it, focus on improving the manipulation phase expressions and the transition logic between approach and manipulation.

(3) If actions seem too noisy or unstable, consider tightening the std parameter range or reducing its initial value.

(4) If the agent overshoots or oscillates, consider adding damping terms or reducing movement weights.

(5) Verify that the correct observation indices are being used — check that positions extracted from obs match the documented observation structure.

(6) Consider whether the gripper action (typically the last action dimension) is being controlled appropriately for the task (open vs closed gripper, timing of grasp).

(7) Review whether the parameter initial values and ranges are appropriate given the observed behavior.

Rewrite the complete GeneratedPolicy class incorporating your improvements.